---
description: Windows 路径强制规范（仅约束 Cursor 文件工具 path/file_path）
alwaysApply: true
---

# Windows Path Discipline

> 操作层细则；规范入口见 `AGENTS.mdc` §17。  
> **Scope**：`Read` / `Write` / `StrReplace` / `Glob` / `Delete` 等工具的 `path` / `file_path`。  
> **Non-scope**：`Shell` 命令字符串内部路径（见 `windows-shell-discipline.mdc`）。

## 强制规则（文件工具）
- **使用反斜杠 `\` 的 Windows 原生路径**
- 正确：`D:\repos\project\src\file.tsx`
- 错误：`D:/repos/project/src/file.tsx` 或 `/d/repos/project/src/file.tsx`

## MINGW / Git Bash 路径转换（喂给文件工具前必须做）

| Git Bash 格式       | Windows 原生格式          |
|---------------------|---------------------------|
| `/c/Users/name/`    | `C:\Users\name\`          |
| `/d/repos/project/` | `D:\repos\project\`       |
| `/s/work/code/`     | `S:\work\code\`           |
| `/mnt/c/Windows/`   | `C:\Windows\`             |

转换：取盘符字母大写 + 冒号，其余 `/` → `\`。

## 其他
- 优先绝对路径
- 禁止把未展开的 `$HOME`、`%USERPROFILE%` 直接塞进 file_path
- UNC：`\\server\share\...`

## 报错时
出现 `ENOENT` / no such file：
1. 是否误用了正斜杠或 MINGW 格式
2. 转成 Windows 原生后重试
3. 必要时用当前 shell 确认 cwd 再拼路径
